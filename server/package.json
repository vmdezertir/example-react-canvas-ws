{
  "name": "server",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node app.js",
    "server": "nodemon app.js",
    "client": "yarn run start --cwd ../client",
    "clientinstall": "yarn install --cwd ../client",
    "allinstall": "yarn install && yarn run clientinstall",
    "dev": "concurrently -r -k \"yarn run server\"  \"yarn run client\""
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@fastify/cors": "^8.3.0",
    "@fastify/helmet": "^10.1.1",
    "@fastify/static": "^6.10.2",
    "@fastify/websocket": "^8.1.0",
    "fastify": "^4.18.0",
    "nanoid": "^4.0.2"
  },
  "devDependencies": {
    "concurrently": "^8.2.0"
  }
}
